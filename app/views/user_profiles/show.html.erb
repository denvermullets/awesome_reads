<h1><%= @user_profile.name %></h1>
    <h5> Bio: <%= @user_profile.bio %></h5>

You have reviewed: 
<ul>
<% @user_profile.books.each do |book|%>
    <li><%= link_to book.title, review_path(book) %></li>
    <% end %>
</ul>